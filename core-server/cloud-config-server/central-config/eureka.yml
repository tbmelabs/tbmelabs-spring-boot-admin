eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false
security:
  oauth2:
    client:
      client-id: ${CLIENT_ID}
      client-secret: ${CLIENT_SECRET}
spring:
  application:
    name: eureka-peer-1
  security:
    user:
      name: ${eureka.administrator.name}
      password: ${eureka.administrator.password}
      roles: CLIENT
server:
  port: 8761
// @flow

export const SET_AUTHENTICATION;: string = '[Authentication] Set';
export const REQUEST_AUTHENTICATION;: string = '[Authentication] Request';
// TODO: Authentication fetch failed (server not reachable)